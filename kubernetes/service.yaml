apiVersion: v1
kind: Service
metadata:
  name: ruby-service
  namespace: ruby
spec:
  selector:
    app: ruby-deployment
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  type: LoadBalancer